{
  "name": "opensim",
  "version": "0.2.2",
  "description": "Open-source browser flight simulator",
  "type": "module",
  "scripts": {
    "hub":  "node server/hub.js",
    "test": "node --test tests/db601-synth.test.mjs && playwright test",
    "version": "git add index.html"
  },
  "dependencies": {
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
